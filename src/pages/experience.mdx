---
layout: ../layouts/Layout.astro
title: "Experience | David Eiken"
description: "Berufserfahrung, Timeline und Meilensteine."
navTitle: "Experience"
navOrder: 2
---

import PageHeader from "../components/PageHeader.astro";
import Timeline from "../components/Timeline.astro";
import TimelineItem from "../components/TimelineItem.astro";
import experienceData from "../data/experience.de.json";
import { processTimelineData } from "../utils/timeline";

export const processedData = processTimelineData(experienceData);

<PageHeader
  title="Architektur mit Struktur und Absicht."
  lead="Ich entwerfe Frontend-Architekturen, die Klarheit schaffen: sauberes HTML, langlebiges CSS, TypeScript als Sicherheitsnetz und Stacks, die zu Team, Produkt und Geschäftszielen passen."
/>

<Timeline headingLeft="Selbstständig" headingRight="Angestellt">
  {processedData.map((item) => (
    <TimelineItem
      date={item.date}
      title={item.title}
      company={item.company}
      impact={item.impact}
      type={item.type}
      isOffset={item.isOffset}
      gridRow={item.gridRow}
    />
  ))}
</Timeline>
